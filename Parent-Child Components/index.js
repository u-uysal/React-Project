import React from "react"
import ReactDOM from "react-dom"

import App from "./App" //Attention : We used only App component, not others.Because App is parent

ReactDOM.render(<App />, document.getElementById("root"))